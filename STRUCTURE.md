# بنية أطياف الممر

التطبيق ثابت بلا خادم: `index.html` للإطار، `style/main.css` للتصميم، و`js/main.js` لمنطق القصة. تحفظ حالة اللعب محليًا في `localStorage`. القراءة الصوتية تستخدم `SpeechSynthesis`، والكاميرا تستخدم `getUserMedia` ولا ترسل البث خارج المتصفح. الأصول محفوظة تحت `assets/`.
