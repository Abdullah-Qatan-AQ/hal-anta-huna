# بنية أطياف الممر

التطبيق ثابت بلا خادم: `index.html` للإطار، `style/main.css` للتصميم، و`js/main.js` لمنطق القصة. تحفظ حالة اللعب والإعدادات محليًا في `localStorage`. زر «قراءة المشهد» يشغّل تسجيل المشهد المدمج من ملفات WAV المحلية؛ لا تستخدم اللعبة `SpeechSynthesis` أو `AudioContext` أو أي توليد صوت/موسيقى/صور من المتصفح. الكاميرا اختيارية وتستخدم `getUserMedia` ولا ترسل البث خارج المتصفح. الأصول محفوظة تحت `assets/`، و`npm run cap:sync` ينسخ الموقع والأصول إلى `www/` ثم إلى منصتي Capacitor.
