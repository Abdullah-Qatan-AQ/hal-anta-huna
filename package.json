{
  "name": "atyaaf-al-mamarr",
  "version": "2.0.0",
  "private": true,
  "description": "فيلم رعب تفاعلي مستقل وتطبيق Capacitor",
  "type": "module",
  "scripts": {
    "sync:web": "node scripts/sync-web.mjs",
    "cap:copy": "npm run sync:web && npx cap copy",
    "cap:sync": "npm run sync:web && npx cap sync",
    "test": "node --check js/main.js && npm run sync:web"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/ios": "^8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  }
}
